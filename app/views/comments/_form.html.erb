<%= form_for [card, comment], html: { class: 'comment-form clearfix' } do |f| %>
  <%= f.text_area :text, placeholder: t("comments.create.comment-placeholder"), class: 'new-comment-text' %>
  <%= button_tag t("comments.create.submit"), class: 'comment-button' %>
<% end %>
