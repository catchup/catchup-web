<%= form_for [card, comment], html: { class: "form-create-comment clearfix" } do |f| %>
  <%= f.text_area :text, class: "new-comment-text", placeholder: t("comments.create.comment-placeholder") %>
  <%= f.submit t("comments.create.submit"), class: "create-comment" %>
<% end %>
