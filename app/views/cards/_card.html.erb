<!-- TODO: @as-cii: help getting the link work? -->
<!-- <h3><%= link_to card.title, [card.board, card] %></h3> -->
<a href="http://example.com" class="card card-1 clearfix" data-role="card" data-card-id="<%= card.id %>">
  <p class="text"><%= card.title %></p>
  <%= form_for [:move, card.board, card], remote: true, data: { role: "card-update-form" } do |f| %>
    <%= f.hidden_field :list_id %>
    <%= f.hidden_field :position %>
  <% end %>
</a>
