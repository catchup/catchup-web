<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title).presence || "Task Planner" %></title>
    <meta name="description" content="<%= content_for(:description).presence || "Task Planner" %>">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= stylesheet_link_tag    'standardize', media: 'all', 'data-turbolinks-track' => true %>
    <%# Controller specific stylesheet %>
    <% if TaskPlanner::Application.assets.find_asset "#{controller_name}.css" %>
      <%= stylesheet_link_tag "#{controller_name}" %>
    <% end %>

    <%= csrf_meta_tags %>

    <%# Views can append to head here using `content_for :head do ... end` %>
    <%= content_for :head %>
  </head>
  <body>

    <%= render "header" %>

    <%= render "flashes" %>

    <%= yield %>

    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <script src="//js.pusher.com/2.2/pusher.min.js" type="text/javascript"></script>

    <%# Views can append to body here using `content_for :body do ... end` %>
    <%= content_for :body %>
  </body>
</html>
